// Squad table on TanStack Table (pace/passing/etc). Stub for brevity.
export function SquadTable({ teamId }: { teamId: string }) {
  return <table className="squad" data-team={teamId}>{/* TODO: TanStack Table */}</table>;
}
